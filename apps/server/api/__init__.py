"""hermes-agent-gui — backend API modules.

Phase 1: health, auth (password + token + cookie), oauth/passkeys stubs,
streaming SSE helpers, runtime adapter (echo + gateway + embedded detect),
chat (POST /api/chat/stream).

Phase 2+: sessions, workspace, kanban, cron, skills, mcp, terminal, …
"""

__version__ = "0.1.0-phase-1"
