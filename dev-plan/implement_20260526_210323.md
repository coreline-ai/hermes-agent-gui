# implement_20260526_210323.md

작성 일시: `2026-05-26 21:04:59 KST`
갱신 일시: `2026-05-26 21:27:21 KST`

이 문서는 `docs/review/*.md` 전문가 분석과 추가 우선순위 조정 결과를 바탕으로, Phase 15 Messaging 진입 전에 필요한 **Phase 14.5 — Pre-Phase-15 Hotfix / Production Readiness Gate** 범위를 고정하기 위한 작업 문서다.

## 개발 목적

Phase 0~14 vertical-slice 구현의 완료 정의를 강화하고, Phase 15 Messaging 확장 전에 baseline 결함을 먼저 제거한다. 특히 passkey, SPA serving, lint, PWA cache, exec feature gate, workspace editor, singlefile path를 순서대로 수정한다.

## 개발 범위

- P1-4 CBOR/COSE passkey 버그 수정
- P0-1 SPA 정적 파일 serving 및 Docker/Electron 배포 진입점 복구
- P0-2 lint 인프라 및 CI gate 복구
- P1-3 PWA API cache 제거 또는 안전 endpoint 제한
- P1-5 exec/PTY/cron/swarm feature gate 도입
- P2-6 workspace editor draft overwrite UX 버그 수정
- P2-7 singlefile build 산출물 경로와 실행 경로 일치
- `09`, `11`, `12` 문서에 Phase 14.5 및 강화된 완료 정의 반영

## 제외 범위

- Phase 15 Messaging platform 구현
- Phase 16 Multi-provider/Slash commands 구현
- Phase 18 이후 RAG/Knowledge Graph/Browser-use 구현
- Hermes Agent 본체 수정 또는 fork
- 새로운 외부 LLM API 의존성 추가
- 대규모 UI redesign 또는 Monaco/xterm.js 본격 도입

## 참조 문서

- [13 · Docs 전문가 분석 및 구현 항목 정리](../docs/review/13-expert-analysis-implementation-items.md)
- [09 · Phase 2 ~ 14 구현 요약](../docs/review/09-phase-2-to-14-summary.md)
- [11 · 통합 개발 계획서](../docs/review/11-implementation-plan-full.md)
- [12 · Implementation Plan Checklist](../docs/review/12-impl-plan-checklist.md)
- [README](../README.md)
- [SECURITY](../SECURITY.md)

## 공통 진행 규칙

- 아래 순서를 유지한다. 특히 CBOR/passkey, SPA serving, lint는 뒤 phase의 검증 기반이다.
- 각 Phase는 자체 테스트 완료 후 다음 Phase로 넘어간다.
- 구현 중 발견한 이슈는 해당 Phase의 `이슈 및 수정`에 기록하고 같은 Phase 안에서 해결한다.
- exec/cron/PTY/swarm 보안 정책은 약화하지 않는다.
- `HERMES_GUI_FAKE_BACKEND=echo`에서 전체 테스트가 가능해야 한다.
- Phase 15 기능은 이 문서 완료 전 구현하지 않는다.

## 통합 수정 우선순위

| 순서 | 코드 | 항목 | 작업량 | 차단 효과 | 권장 PR |
|---:|---|---|---:|---|---|
| 1 | P1-4 | CBOR/COSE passkey 버그 | 0.5일 | passkey 기능 자체 불능 차단 | PR-A |
| 2 | P0-1 | SPA serving | 1일 | Docker/Electron/backend-only 배포 불가 차단 | PR-A |
| 3 | P0-2 | lint 인프라 | 0.5일 | CI 품질 gate 복구 | PR-A |
| 4 | P1-3 | PWA API 캐시 제거 | 0.5일 | 민감 API 응답/토큰 영속화 차단 | PR-B |
| 5 | P1-5 | exec feature gate | 1.5일 | Docker/remote bind RCE 차단 | PR-B |
| 6 | P2-6 | workspace editor | 0.5일 | 편집 중 draft overwrite UX 버그 수정 | PR-C |
| 7 | P2-7 | singlefile path | 0.5일 | 문서 ↔ 실행 경로 불일치 제거 | PR-C |
| 8 | DOC | Phase 14.5 문서 갱신 | 병행 | 기존 완료 정의 강화 + Phase 15 readiness | PR-C |

총 코드 작업량: **4.5일**  
추천 병합 전략: **3 PR 하이브리드** 권장. 단, 일정상 빠른 baseline 복구가 목적이면 1개 M-size PR로 묶어도 된다.

## Phase 상태 요약

- [x] Phase 1 완료 — P1-4 CBOR/COSE passkey 버그 수정
- [x] Phase 2 완료 — P0-1 SPA serving 복구
- [x] Phase 3 완료 — P0-2 lint/CI gate 복구
- [x] Phase 4 완료 — P1-3 PWA API cache 제거
- [x] Phase 5 완료 — P1-5 exec feature gate
- [x] Phase 6 완료 — P2-6 workspace editor UX 수정
- [x] Phase 7 완료 — P2-7 singlefile path 정렬
- [x] Phase 8 완료 — 문서 갱신 및 Phase 15 readiness 선언

## Phase 1. P1-4 CBOR/COSE Passkey 버그 수정

### 목표

Passkey/WebAuthn 등록·인증이 실제 authenticator와 호환되도록 CBOR/COSE 파싱 오류를 수정한다.

### 구현 태스크

- [x] `apps/server/api/passkeys.py`의 CBOR negative integer decoding을 spec-correct 방식으로 수정 (`-1 - n`)
- [x] COSE `alg=-7` ES256, `alg=-257` RS256 파싱 테스트 추가
- [x] authData length/offset boundary 검증 추가
- [x] 잘못된 CBOR 입력이 500이 아니라 400 계열로 떨어지도록 예외 처리
- [x] Passkey가 production-ready인지 experimental인지 README/SECURITY 표현 정리

### 자체 테스트

- [x] `cd apps/server && python3 -m pytest tests/test_auth_flow.py -v`
- [x] 신규 passkey CBOR/COSE golden vector 테스트 통과
- [x] malformed attestation/authenticatorData 입력이 400 응답

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. P0-1 SPA Serving 복구

### 목표

Docker, Electron, backend-only 실행에서 사용자가 `/`로 접근했을 때 실제 웹 UI가 열린다.

### 구현 태스크

- [x] `apps/server/server.py`에 `apps/web/dist` 정적 파일 serving 추가
- [x] non-API GET path는 SPA fallback으로 `index.html` 반환
- [x] API 404와 SPA fallback 경계를 명확히 분리
- [x] `Cache-Control` 정책을 HTML/API/assets 별로 분리
- [x] Docker image에서 `/`와 `/api/health` 모두 smoke test
- [x] Electron `main.cjs`가 health 후 렌더링 가능한 URL을 여는지 검증

### 자체 테스트

- [x] `pnpm build`
- [x] `python3 -m pytest apps/server -q`
- [x] backend-only 서버 실행 후 `curl -i http://127.0.0.1:8800/` → 200 `text/html`
- [x] `curl -i http://127.0.0.1:8800/chat` → 200 `text/html`
- [x] `curl -i http://127.0.0.1:8800/api/health` → 200 JSON

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. P0-2 Lint 인프라 / CI Gate 복구

### 목표

`pnpm lint`가 실제로 동작하고 CI에서 품질 gate 역할을 한다.

### 구현 태스크

- [x] ESLint dependency와 flat config 추가
- [x] React/TypeScript lint rule 최소 세트 적용
- [x] generated route tree lint 제외 또는 별도 override 적용
- [x] `pnpm lint` 스크립트가 정상 exit code 반환하도록 수정
- [x] CI `web-build` job에 `pnpm lint` 추가
- [x] 생성물(`dist`, `__pycache__`, `.pytest_cache`) 정리 및 `.gitignore` 검증

### 자체 테스트

- [x] `pnpm lint`
- [x] `pnpm typecheck`
- [x] `pnpm --filter @hermes-agent-gui/web test`
- [x] `python3 -m pytest apps/server -q`

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. P1-3 PWA API Cache 제거

### 목표

Service Worker가 session/memory/log/workspace 같은 민감 API 응답을 Cache Storage에 남기지 않도록 한다.

### 구현 태스크

- [x] `apps/web/vite.config.ts` Workbox runtimeCaching에서 `/api/*` GET cache 제거
- [x] 필요 시 `/api/health`만 NetworkOnly 또는 safe NetworkFirst로 제한
- [x] 기존 `api` cache 삭제 migration 추가
- [x] offline fallback이 API 요청을 HTML로 대체하지 않도록 denylist 재검증
- [x] PWA 문서/README에 API no-store 정책 명시

### 자체 테스트

- [x] `pnpm build`
- [x] 생성된 `dist/sw.js`에 `/api/` NetworkFirst cache가 없는지 확인
- [x] browser smoke: Cache Storage에 `api` cache가 생성되지 않음
- [x] `/api/health`는 온라인에서 정상 응답

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 5. P1-5 Exec Feature Gate

### 목표

terminal, PTY, cron, swarm, 향후 browser-use/CLI bridge가 공통 보안 정책 아래에서만 실행된다.

### 구현 태스크

- [x] `HERMES_GUI_ENABLE_EXEC` 또는 도메인별 enable flag 설계
- [x] remote bind 상태에서는 exec 계열 기본 비활성화
- [x] terminal one-shot exec에 feature gate 적용
- [x] PTY create/input/resize/delete에 feature gate 적용
- [x] cron create/run-now에 feature gate 또는 unsafe command policy 적용
- [x] swarm worker spawn/dispatch에 feature gate 적용
- [x] PTY cwd를 workspace `_safe_path()`로 제한
- [x] exec 감사 로그 + redaction 적용
- [x] UI에서 disabled 상태와 enable 방법을 명확히 표시

### 자체 테스트

- [x] flag off 상태에서 terminal/PTY/cron/swarm 실행 API가 403 `exec_disabled` 응답
- [x] flag on + authenticated + local bind에서 기존 happy path 동작
- [x] remote bind + flag off에서 RCE 표면 차단
- [x] bad cwd/path traversal 거부
- [x] `python3 -m pytest apps/server -q`

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 6. P2-6 Workspace Editor Draft Overwrite 수정

### 목표

파일 편집 중 서버 read 결과가 render 중 `setDraft()`를 반복 호출해 사용자 입력을 덮어쓰는 문제를 해결한다.

### 구현 태스크

- [x] `apps/web/src/routes/workspace.tsx`의 render-time `setDraft()` 제거
- [x] `useEffect`로 selected/read.data 변경 시에만 draft 초기화
- [x] dirty state를 추가해 사용자가 편집 중이면 자동 덮어쓰기 방지
- [x] selected file 변경 시 dirty/reset/save 상태 정리
- [x] 저장 성공 후 dirty false 처리

### 자체 테스트

- [x] workspace editor unit/component test 추가 또는 수동 smoke 기록
- [x] 파일 선택 → draft 표시
- [x] draft 수정 중 query refetch 발생 → 사용자 입력 보존
- [x] 저장 후 list/read invalidate 정상 동작
- [x] `pnpm typecheck && pnpm --filter @hermes-agent-gui/web test`

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 7. P2-7 Singlefile Path 정렬

### 목표

`pnpm build:singlefile` 산출물과 `serve_singlefile.py` 기본 실행 경로가 일치한다.

### 구현 태스크

- [x] `serve_singlefile.py`가 기본적으로 `apps/web/dist/index.html`을 찾도록 수정하거나 build script가 `hermes-agent-gui.html`을 생성하도록 정렬
- [x] README singlefile 실행 명령 정정
- [x] missing HTML 에러 메시지를 명확히 개선
- [x] 일반 SPA serving과 singlefile serving의 차이를 문서화

### 자체 테스트

- [x] `pnpm --filter @hermes-agent-gui/web build:singlefile`
- [x] `python3 apps/server/serve_singlefile.py --port 18800` 실행
- [x] `curl -i http://127.0.0.1:18800/` → 200 `text/html`
- [x] `curl -i http://127.0.0.1:18800/api/health` → 200 JSON

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 8. 문서 갱신 / Phase 15 Readiness

### 목표

Phase 14.5 hotfix를 마스터 플랜에 반영하고, Phase 15 진입 조건을 명확히 한다.

### 구현 태스크

- [x] `docs/review/09-phase-2-to-14-summary.md`에 Phase 14.5 결과와 강화된 완료 정의 추가
- [x] `docs/review/11-implementation-plan-full.md` 공통 표준/보안/PWA/배포 항목에 SPA serving, lint, NetworkOnly, exec gate 반영
- [x] `docs/review/12-impl-plan-checklist.md`에 신규 Phase 14.5 추가
- [x] README Quick start에 backend-only, web dev, Docker, Electron, singlefile 실행 경로 차이 명시
- [x] SECURITY.md에 passkey/exec/cron/PTY/swarm threat model 추가
- [x] Phase 15a 시작 조건 체크리스트 추가

### 자체 테스트

- [x] 문서의 주요 명령어를 최소 smoke로 검증
- [x] `pnpm lint && pnpm typecheck && pnpm build`
- [x] `pnpm --filter @hermes-agent-gui/web test`
- [x] `python3 -m pytest apps/server -q`
- [x] Docker smoke: `/`, `/api/health`

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 15a 진행 가능


## 구현/검증 결과 (2026-05-26)

- Passkey CBOR/COSE parser 수정 및 ES256/RS256/malformed regression 추가
- 서버 SPA serving + HEAD/static cache 정책 + Docker/Electron/backend-only 진입점 복구
- ESLint flat config/dependency 추가 및 CI lint gate 반영
- Workbox `/api/*` runtime cache 제거 + `sw-cleanup.js`로 기존 `api` cache 삭제
- terminal/PTY/cron/swarm 공통 exec feature gate 추가 (`HERMES_GUI_ENABLE_EXEC`, `HERMES_GUI_ALLOW_REMOTE_EXEC`)
- workspace editor dirty draft 보호 적용
- singlefile 기본 경로를 `apps/web/dist/index.html`로 정렬
- README/SECURITY/09/11/12 문서 동기화

### 완료 검증

```bash
python3 -m pytest apps/server -q                      # 51 passed
pnpm lint                                             # passed
pnpm typecheck                                        # passed
pnpm --filter @hermes-agent-gui/web test              # 13 passed
pnpm build                                            # passed
pnpm --filter @hermes-agent-gui/web build:singlefile  # passed
Docker build + / + /api/health smoke                  # passed
backend-only / + /chat + /api/health smoke            # passed
singlefile / + /api/health smoke                      # passed
```

## PR 분할 권장

### 추천: 3 PR 하이브리드

| PR | 포함 항목 | 이유 |
|---|---|---|
| PR-A | Phase 1~3: CBOR, SPA serving, lint | 즉시 차단 해소 + CI 기반 복구 |
| PR-B | Phase 4~5: PWA cache, exec gate | 보안 성격이 강해 별도 리뷰 권장 |
| PR-C | Phase 6~8: editor, singlefile, docs | UX/문서 정렬 마무리 |

### 대안

- **1 PR**: 7개를 한 번에 처리. 빠른 baseline 복구에는 좋지만 리뷰 밀도가 떨어질 수 있다.
- **7 PR**: 각 항목 S-size로 분리. 가장 안전하지만 관리 비용이 증가한다.

## 최종 완료 기준

- [x] 신규/수정 테스트 포함해 서버 테스트 약 50 cases 수준으로 증가
- [x] `python3 -m pytest apps/server -q` 통과
- [x] `pnpm lint` 통과
- [x] `pnpm typecheck` 통과
- [x] `pnpm --filter @hermes-agent-gui/web test` 통과
- [x] `pnpm build` 통과
- [x] Docker image에서 `/`와 `/api/health` smoke 통과
- [x] Phase 15a 시작 조건 문서화 완료
