# Phase 16 Implementation — Multi-provider LLM + Slash Commands

Date: 2026-05-26
Status: ✅ Implemented + verified

## Implemented

- Provider backend
  - `apps/server/api/providers/models.py` — provider/model dataclasses and preset kind enum.
  - `apps/server/api/providers/catalog.py` — 14 preset provider metadata.
  - `apps/server/api/providers/store.py` — SQLite provider CRUD + atomic `~/.hermes/.env` API key storage.
  - `apps/server/api/providers/discovery.py` — OpenAI-compatible, Anthropic static, Google, Ollama discovery with 5-minute cache.
  - `apps/server/api/providers/oauth/` — PKCE S256 OAuth state with 10-minute TTL for Nous Portal/OpenAI Codex.
  - `apps/server/api/providers/routes.py` — presets/list/create/delete/models/test/OAuth routes.
- Slash backend
  - `apps/server/api/slash_commands.py` — 22 command registry, parser, stub executor for `/usage`, `/compact`, `/model`, `/status`.
- Frontend
  - `apps/web/src/routes/providers.tsx` — provider add/list/test/models UI.
  - `apps/web/src/components/chat/model-picker.tsx` — model dropdown using discovered provider models.
  - `apps/web/src/components/chat/slash-menu.tsx` — slash autocomplete.
  - `apps/web/src/lib/slash-commands.ts` — 22 command definitions + parser + matching.
  - `apps/web/src/routes/chat.tsx` — `/model`, `/usage`, `/help`, navigation slash handling and model field propagation.
- i18n/docs
  - en/ko slash/provider keys.
  - README, SECURITY, implementation docs/checklist updated.

## Verification

- `python3 -m pytest apps/server -q` → 93 passed.
- `pnpm --filter @hermes-agent-gui/web test` → 19 passed.
- `pnpm lint` → passed.
- `pnpm typecheck` → passed.
- `pnpm build` → passed.

## Next

- Phase 17 — Persona SOUL + FTS5 Search + Usage Analytics.
