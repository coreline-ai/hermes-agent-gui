# implement_20260526_214214_phase15b.md

작성 일시: `2026-05-26 21:42:14 KST`

## 개발 목적

Phase 15a messaging foundation 위에 **Phase 15b — Direct Webhook + Home Assistant runtime**을 구현한다. Webhook/Home Assistant는 Hermes Agent 본체 없이도 hermes-agent-gui backend 단독으로 동작해야 한다.

## 구현 범위

- [x] `api/messaging/platforms/webhook.py` — random token registration, HMAC-SHA256 signature verification, 256KB payload limit, 60/min rate limit
- [x] `api/messaging/webhook_inbound.py` — `POST /api/messaging/webhook/{token}/inbound` route shim
- [x] Webhook inbound → runtime adapter chat 호출 → JSON response 반환
- [x] `api/messaging/platforms/home_assistant.py` — HA `/api/services/{domain}/{service}` notify REST 호출
- [x] `messaging_status` last_event/status update
- [x] Phase 15b tests 추가
- [x] README/SECURITY/11/12 문서 동기화

## 제외 범위

- Messaging frontend card/drawer UI
- Profile archive export/import/clone
- Webhook callback URL outbound fan-out
- Home Assistant inbound automation endpoint

## 검증 결과

```bash
python3 -m pytest apps/server/tests/test_platform_webhook.py apps/server/tests/test_platform_home_assistant.py -q
# 6 passed

python3 -m pytest apps/server -q
# 75 passed
```

추가 검증은 최종 gate에서 `pnpm lint`, `pnpm typecheck`, `pnpm --filter @hermes-agent-gui/web test`, `pnpm build`로 수행한다.

## 다음 순서

1. Phase 15c — Messaging frontend + Profile Archive
2. Phase 16 — Multi-provider LLM + Slash Commands
