# implement_20260526_224900_phase18_to_25

## Scope
Phase 18부터 Phase 25까지 Hermes Agent 전용 GUI의 고급 페이지/백엔드 세로 슬라이스를 순차 구현했다.

## Implemented
- [x] Phase 18 — Auto-Compress + RAG
  - `apps/server/api/compression/` trigger/summarizer/embedder/vss_store/inject/routes
  - `/api/sessions/{sid}/compact`, `/api/sessions/{sid}/memory`, `/api/memory/search`
  - `chat.py` adapter-only RAG injection, visible transcript unchanged
  - `/rag` page
- [x] Phase 19 — Memory Providers + PII
  - `apps/server/api/pii.py` + custom regex guard
  - `apps/server/api/memory_providers/` local_vss + 6 external adapters
  - chat PII SSE event + inline badge
- [x] Phase 20 — Groups + Backup + Updater
  - `apps/server/api/groups/`, `backup.py`, `debug_dump.py`
  - Electron updater bridge + renderer toast
  - `/groups` page
- [x] Phase 21 — GBrain
  - `apps/server/api/brain/` extractor/graph/traversal/synthesis/routes
  - `/brain` page
- [x] Phase 22 — Code Knowledge Graph
  - `apps/server/api/codegraph/` with Python/TS/JS/Go/Rust parsers
  - `/code-graph` page
- [x] Phase 23 — Browser Use
  - `apps/server/api/browser/` allowlist/private-IP guard/session/actions/routes
  - `/browser` page
- [x] Phase 24 — UX Quick Wins
  - `react-virtuoso` chat virtualization threshold
  - sessions source grouping
  - `apps/server/cli.py` maintenance CLI + `pyproject.toml`
  - login-lock settings UI
- [x] Phase 25a/b/c — Office 3D + Multi-CLI + Marketplace
  - feature-flagged lazy 3D office under `apps/web/src/feature-3d/`
  - `apps/server/api/cli_bridges/` + `/cli-bridges`
  - `apps/server/api/marketplace/` 30 curated presets + `/marketplace`

## Verification
- [x] `python3 -m pytest apps/server -q` → 133 passed
- [x] `pnpm --filter @hermes-agent-gui/web test` → 20 passed
- [x] `pnpm lint` → passed
- [x] `pnpm typecheck` → passed
- [x] `pnpm build` → passed

## Notes
- `sqlite-vss`, `tree-sitter`, and Playwright are documented as optional backends; implementation includes dependency-free deterministic fallbacks for local CI and Hermes-not-installed environments.
- Repository directory is not a git repository, so branch/commit/PR creation was not possible in this workspace.


## 2026-05-27 Internal verification follow-up
- [x] MVP internal E2E: login → session create → echo SSE → persisted restore
- [x] Phase 15 internal E2E: Telegram delegated 503, webhook signed inbound echo, profile archive export/import relogin flag
- [x] Phase 16 internal E2E: OpenAI `/v1/models` mock discovery + `/model` slash parse
- External real-service smoke remains intentionally excluded by user request.
