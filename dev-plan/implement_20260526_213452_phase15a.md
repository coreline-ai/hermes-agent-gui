# implement_20260526_213452_phase15a.md

작성 일시: `2026-05-26 21:34:52 KST`

## 개발 목적

Phase 14.5 완료 후 다음 순서인 **Phase 15a — Messaging Foundation + 14 Delegated Platforms**를 구현한다. Hermes Agent 본체가 실제 메시징 런타임을 담당하고, hermes-agent-gui는 platform registry, credential 저장, behavior YAML, 상태 캐시, 위임 test probe, HTTP API를 제공한다.

## 구현 범위

- [x] `apps/server/api/messaging/models.py` — dataclass 모델
- [x] `apps/server/api/messaging/registry.py` — 16 platform registry, 14 delegated + 2 direct
- [x] `apps/server/api/messaging/credentials.py` — `~/.hermes/.env` atomic write/merge + 0600
- [x] `apps/server/api/messaging/behavior.py` — `~/.hermes/config.yaml` platform behavior read/write
- [x] `apps/server/api/messaging/status.py` — `messaging_status` SQLite schema + status/event helpers
- [x] `apps/server/api/messaging/delegate_probe.py` — Hermes `/v1/messaging/test/{platform}` 위임 probe
- [x] `apps/server/api/messaging/platforms/*.py` — 14 delegated thin wrappers + 2 direct placeholders
- [x] `apps/server/api/messaging/routes.py` — list/configure/test/delete routes
- [x] `apps/server/server.py` — messaging router wiring
- [x] tests 추가: registry, credentials, delegated validation/endpoint/status

## 제외 범위

- Phase 15b direct Webhook inbound runtime
- Phase 15b Home Assistant notify REST 호출
- Phase 15c messaging frontend route/components
- Phase 15c profile archive export/import/clone

## 검증 결과

```bash
python3 -m pytest apps/server/tests/test_messaging_registry.py apps/server/tests/test_messaging_creds.py apps/server/tests/test_platforms_delegated.py -q
# 18 passed

python3 -m pytest apps/server -q
# 69 passed
```

## 다음 순서

1. Phase 15b — Direct Webhook + Home Assistant runtime
2. Phase 15c — Messaging frontend + Profile Archive
3. Phase 16 — Multi-provider LLM + Slash Commands
