# Phase 17 Implementation — Persona SOUL + FTS5 Search + Usage Analytics

Date: 2026-05-26
Status: ✅ Implemented + verified

## Implemented

- Persona SOUL
  - `apps/server/api/persona.py`
  - `apps/server/api/presets/personas.py`
  - `/api/persona`, `/api/persona/presets`
  - 6 presets: Sage, Trader, Builder, Scribe, Ops, Coder
  - 100KB SOUL.md limit
  - `apps/web/src/routes/persona.tsx`
- FTS5 Search
  - `apps/server/api/sessions/search.py`
  - FTS5 virtual table + metadata table
  - incremental `_index_messages()` hook in `SessionStore.append_messages()`
  - idempotent backfill at server startup and `/api/sessions/search/backfill`
  - `/api/sessions/search?q=...`
  - redacted + HTML-escaped snippets
  - `apps/web/src/components/global-search.tsx` Cmd/Ctrl+K modal with 200ms debounce
- Usage Analytics
  - `apps/server/api/usage.py`
  - `usage_turns` table, cost calculator, token estimate, `/api/usage/summary`
  - chat hook records approximate per-turn usage when a session is attached
  - `apps/web/src/routes/usage.tsx` Recharts 30-day bar + model pie
  - `apps/web/src/lib/usage-pricing.ts`
- Navigation/i18n/docs
  - Persona and Usage nav items
  - en/ko i18n keys
  - README, SECURITY-adjacent docs/checklist updated

## Verification

- `python3 -m pytest apps/server -q` → 100 passed.
- `pnpm --filter @hermes-agent-gui/web test` → 20 passed.
- `pnpm lint` → passed.
- `pnpm typecheck` → passed.
- `pnpm build` → passed.

## Next

- Phase 18 — Auto-Compress + RAG.
